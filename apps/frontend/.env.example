# Frontend environment variables
# Copy to .env.local and fill with real values.

# Backend API base URL. In dev with next.config.ts rewrites this can be omitted
# (defaults to http://localhost:3001). In production point to the backend host.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001